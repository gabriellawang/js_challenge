import {makeStyles} from "@material-ui/core/styles";

const useStyle = makeStyles({
  wrapper: {
    marginTop: "40px"
  },
  menuList: {
    marginTop: "50px"
  },
  basket: {
    margin: "25px",
    marginTop: "40px"
  }
});

export default useStyle;
